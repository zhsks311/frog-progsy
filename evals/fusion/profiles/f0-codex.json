{
  "name": "f0-codex",
  "description": "F0 variant with a codex-only panel for periods when the anthropic lane is down. Current fusion defaults otherwise (task-only pre-final context, default 15s buffered stage timeout, no judge/synthesizer pin - loud first-panel-member fallback is the current default semantics).",
  "targetModel": "frogp/mix",
  "modelMixing": {
    "enabled": true,
    "combine": "fusion",
    "agents": [
      {
        "provider": "codex",
        "model": "gpt-5.5"
      },
      {
        "provider": "codex",
        "model": "gpt-5.4"
      },
      {
        "provider": "codex",
        "model": "gpt-5.4-mini"
      }
    ]
  }
}
